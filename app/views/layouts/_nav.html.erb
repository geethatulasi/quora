<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button class="navbar-collapsed collapse" data-toggle="navbar" data-target="#navbar"
      aria-expanded="false" aria-controlls="navbar">
        <span class="sr-only">toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Home",root_path,class: 'navbar-brand' %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
          <% if current_user.profile == nil %>
            <li><%= link_to "Create Profile",new_profile_path %></li>
          <% else %>
            <li><%= link_to current_user.name,current_user.profile %></li>
          <% end %>
          <li><%= link_to "Create Question",new_question_path %></li>
          <li><%= link_to "Logout",destroy_user_session_path,method: :delete %></li>
        <% else %>
          <li><%= link_to "Login",new_user_session_path %></li>
          <li><%= link_to "SignUp",new_user_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>